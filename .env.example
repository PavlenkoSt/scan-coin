# Mobile app runtime config
# mock | remote
EXPO_PUBLIC_COIN_PROVIDER=mock

# For remote mode, set to deployed backend base URL (without trailing slash)
# Example: https://scan-coin-api.vercel.app
EXPO_PUBLIC_API_BASE_URL=

# Backend (Vercel / server)
OPENAI_API_KEY=
OPENAI_VISION_MODEL=gpt-4o-mini
